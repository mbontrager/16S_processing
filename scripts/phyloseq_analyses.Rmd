`r knitr::opts_chunk$set(cache=TRUE, fig.width=10)`

plot_bar function: Powerful, flexible phyloseq bar plots
========================================================

## 16S analysis with Phyloseq and ggplot2. Installation instructions are online

Load the dataset, and trim to just the *Chlamydiae* phylum.

```{r, message=FALSE, warning=FALSE}
library("phyloseq"); packageVersion("phyloseq")
library("ggplot2"); packageVersion("ggplot2")
```

ggplot2 package theme set. See [the ggplot2 online documentation](http://docs.ggplot2.org/current/) for further help.

```{r ggplot2-theme-set}
theme_set(theme_bw())
```



```{rgp-sample-names}
myData
ntaxa(myData)
nsamples(myData)
sample_names(myData)
rank_names(myData)
sample_variables(myData)
otu_table(myData)[1:5, 1:5]
tax_table(myData)[1:5, 1:4]
```

